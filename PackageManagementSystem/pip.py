# a package is a collection of individual files and information about those files.
# The files are packed together as a software collection that provides certain functionality as part of a larger system.

# by package manager we can download package form software repositories and then install it
# we can find pip by command where pip

# pip install yolk3k
# it is a package by which we can see install package . also pip freeze do same work

